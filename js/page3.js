// Placeholder – AI / Ödeme / 3D akışları bağlanacak

document.querySelectorAll("button").forEach(btn => {
    btn.addEventListener("click", () => {
        console.log("Buton aktif:", btn.innerText);
    });
});
